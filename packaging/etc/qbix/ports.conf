{
    "_comment": "Listen ports (like Apache's ports.conf). Add \"web\": {\"https\": {\"port\": 8443}} for HTTPS; docs/https.md.",
    "Q": {
        "webserver": {
            "port": 8080
        }
    }
}
